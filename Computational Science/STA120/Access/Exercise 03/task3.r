png(file="solution.png")
set.seed(12)
n <- ...

# Sample the data
hist( ... , probability = TRUE, main = "") # Histogram of the sampled data with a superimposed theoretical density. 

# Using curve() function plot the theroretical function. For more info: ?curve()
...

dev.off()